# Supabase credentials (optional for demo / offline mode)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Cloudflare Worker submit endpoint (optional, defaults to local mock API or Supabase direct)
NEXT_PUBLIC_SUBMIT_WORKER_URL=
